'use strict';

var React = require('react-native');
var {
  AppRegistry,
} = React;

var ReactTestProject = require('./MainScreen');

AppRegistry.registerComponent('ReactTestProject', () => ReactTestProject);
